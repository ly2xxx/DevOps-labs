apiVersion: v1
kind: Pod
metadata:
  name: {{ include "webserver.fullname" . }}-curlpod
  labels:
    {{- include "webserver.labels" . | nindent 4 }}
spec:
  containers:
  - name: curlpod
    image: curlimages/curl
    command: ["sh", "-c", "while true; do sleep 3600; done"]
